<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doit">

    <!-- Permission for posting notifications (Required for Android 13 and above) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />


    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">

        <!-- SplashScreen Activity -->
        <activity android:name=".SplashScreen"
            android:exported="true"> <!-- 'exported' attribute is required for apps targeting Android 12 and above if the activity is accessible by other apps or is the entry point -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- UpdateCard Activity -->
        <activity
            android:name=".UpdateCard"
            android:windowSoftInputMode="adjustResize"
            android:exported="true"> <!-- 'exported' explicitly set to true as this activity might be launched from notifications or other external sources -->
        </activity>

        <!-- CreateCard Activity -->
        <activity android:name=".CreateCard"
            android:exported="true" >
        </activity> <!-- Make sure this is set to true if targeting Android 12 or higher -->


        <!-- MainActivity -->
        <activity android:name=".MainActivity"
            android:exported="true"> <!-- 'exported' set to true, necessary for apps targeting Android 12 and above -->
        </activity>

        <!-- Notification Receiver -->
        <receiver android:name=".ReminderBroadcastReceiver" />



    </application>
</manifest>
